# AutoMark 试卷批改软件 —— 产品开发文档

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v0.1（立项版） |
| 文档状态 | 待评审（含「待确认」项，见 §12） |
| 编制日期 | 2026-08-19 |
| 文档用途 | 内部研发立项（不对外） |
| 目标读者 | 研发团队、产品负责人 |
| 关联代码目录 | 本目录（`C:/Users/31672/Desktop/PLAYGROUND/AutoMark`） |

---

## 目录

1. [产品概述](#1-产品概述)
2. [版本规划与范围](#2-版本规划与范围)
3. [核心工作流](#3-核心工作流)
4. [功能需求详述（生成）](#4-功能需求详述试卷答题卡生成)
5. [功能需求详述（批改）](#5-功能需求详述试卷答题卡批改)
6. [非功能需求](#6-非功能需求)
7. [技术架构与实现方案](#7-技术架构与实现方案)
8. [核心算法与识别细节](#8-核心算法与识别细节)
9. [数据格式定义](#9-数据格式定义)
10. [测试与验收](#10-测试与验收)
11. [风险与应对](#11-风险与应对)
12. [分阶段计划与里程碑](#12-分阶段计划与里程碑)
13. [附录](#13-附录)

---

## 1. 产品概述

### 1.1 背景与目标

**AutoMark** 是一款面向**个人教师**的试卷批改软件，围绕两个核心能力形成闭环：

```
① 组卷（生成） ──► 打印/分发 ──► 学生作答 ──► 扫描回收 ──► ② 批改 ──► 数据导出
```

- **生成**：内置高易用性 GUI 试卷编辑器（拖拽式组卷），或直接导入特定格式的 JSON 文件（AMF，见 §9.1），一键生成**试卷 PDF、配套答题卡 PDF、答案 PDF** 及原始 JSON。
- **批改**：教师提交扫描 PDF 与答案，软件借助页面上的**定位标记 / 二维码 / 条码**完成版面定位与题目分割；**选择题涂卡通过像素分析**识别；**填空题通过 OCR 转写**；随后按答案自动判分，可导出**学生答案数据、学生成绩数据、班级成绩报告**（统一 XLSX 格式）。

### 1.2 产品定位

| 维度 | 定位 |
| --- | --- |
| 形态 | 轻量化 **Web 应用**，运行在本地端口（`127.0.0.1:<port>`），由**启动器**（桌面封装）拉起并打开浏览器访问 |
| 用户 | 单机单用户（个人教师），无账号体系、无云同步（首版） |
| 学科 | 引擎与学科无关，可配置任意学科任意学段；**首版以英语学科为主**，故不做 LaTeX 类复杂排版 |
| 数据处理 | **本地优先**：OCR、PDF 生成默认全部本地完成（开源组件），数据不出本机；仅当用户在设置中显式配置外部 OCR / 视觉 LLM API 时，指定环节才走云端 |

### 1.3 目标用户与典型场景

- **用户**：中小学/培训机构个人教师，日常小测、单元考、月考，通常一次 30–120 名学生。
- **典型场景**：教师周末组卷 → 打印试卷+答题卡 → 周一考试 → 课后用扫描仪/一体机扫描成 PDF → 打开 AutoMark 上传扫描件与答案 → 几分钟内得到批改结果与班级报告 → 导出 XLSX 存档或导入校务系统。

### 1.4 与现有方案对比（立项依据）

| 方案 | 局限 | AutoMark 的差异点 |
| --- | --- | --- |
| 硬件光标阅读机（OMR） | 需专用设备与答题卡纸，贵，主观题仍要人工 | 纯软件方案，普通 A4 纸 + 普通扫描仪即可 |
| 通用 OCR/网盘工具 | 无版面定位、无批改逻辑、无成绩汇总 | 内置定位标记体系、自动判分与班级报表 |
| 大型在线阅卷平台 | 需网络、按账号计费、数据上云、操作重 | 本地离线、一次付费/免费、轻量单机 |

---

## 2. 版本规划与范围

### 2.1 首版（MVP，v0.1）范围

| 模块 | 首版包含 |
| --- | --- |
| 组卷 | GUI 试卷编辑器（拖拽题型到画布、逐题设置）；AMF JSON 导入（带 schema 校验与可读错误提示） |
| 题型 | 单选、多选（含不定项）、判断、填空（见 §4.1.2 记分规则） |
| 作答方式 | 模式 A：标准化答题卡作答（默认）；模式 B：直接在试卷上作答（答题处内嵌标记块） |
| PDF | 试卷 PDF、答题卡 PDF、答案 PDF（含答案元数据，供批改） |
| 识别 | 定位标记 + 二维码；倾斜校正与透视矫正；题块分割；涂卡像素分析；填空 OCR 转写 |
| 批改 | 客观题自动判分；置信度分级；低置信度人工复核界面 |
| 数据 | 考试项目以本地目录+JSON 存储；导出统一格式 XLSX（学生答案/学生成绩/班级报告） |

### 2.2 首版明确不做（Out of Scope）

- ❌ 主观题（简答/作文）自动批改、AI 评分（用户配置的视觉 LLM 仅用于**转写扫描文件**，不参与判分）
- ❌ 多人协同阅卷、教师协作
- ❌ 长期数据管理：题库管理、历史考试存档、成绩趋势分析（导出数据格式先行统一，存档分析放 v0.2+）
- ❌ LaTeX / 复杂公式排版（英语学科不需要；后续科目扩展再评估）
- ❌ 移动端、多用户、云同步、账号体系
- ❌ 一页扫描件含多份答题卡的自动切分（见 §12 待确认项 F-2）

### 2.3 后续版本展望（v0.2+，仅规划方向）

历史考试存档与重导出、成绩趋势/班级对比图表、题库与常用模板、连线/排序/作文题型、主观题人工阅卷工作台、扫描件按页切分、启动器自动更新、macOS 支持。

---

## 3. 核心工作流

### 3.1 角色

- **教师**：唯一角色，完成全部操作（组卷、导入、上传扫描件、复核、导出）。

### 3.2 主流程（端到端）

```
┌─ 组卷 ───────────────────────────────────────────────┐
│ 1. 新建考试项目（选学科/学段/考试标题，可选导入班级名单）│
│ 2. 组卷：GUI 编辑器拖拽组卷  或  导入 AMF JSON         │
│ 3. 预览 → 生成 PDF 三件套（试卷/答题卡/答案）+ AMF JSON │
└──────────────────────┬───────────────────────────────┘
                       ▼
                 4. 打印、分发、考试（离线环节）
                       ▼
┌─ 批改 ───────────────────────────────────────────────┐
│ 5. 上传扫描 PDF（多页，每页对应一名学生的作答）        │
│ 6. 图像预处理（灰度/二值化/倾斜校正/透视矫正）          │
│ 7. 定位标记识别 → 版面配准 → 题目分割                  │
│ 8. 涂卡像素分析（客观题）+ OCR 转写（填空）             │
│ 9. 答案匹配 → 判分 → 置信度分级                        │
│ 10. 人工复核队列（低置信度条目，教师修正）              │
│ 11. 导出：学生答案 / 学生成绩 / 班级报告（XLSX）+ 结果JSON│
└──────────────────────────────────────────────────────┘
```

### 3.3 批改的输入与输出

| 输入 | 说明 |
| --- | --- |
| 扫描 PDF | 学生作答后的试卷或答题卡扫描件，多页；单页通常即一名学生的作答；黑白/灰度/彩色均可，建议 300 DPI（最低 200 DPI） |
| 答案 | 组卷时已内嵌于 AMF JSON；批改时无需教师再次录入（支持从试卷项目直接关联） |

| 输出 | 说明 |
| --- | --- |
| 批改结果 JSON | 每位学生每题作答原始记录、识别置信度、判定与得分 |
| 导出 XLSX | 三张工作表：学生答案明细、学生成绩、班级报告（见 §9.3） |

---

## 4. 功能需求详述（试卷/答题卡生成）

### 4.1 GUI 试卷编辑器

#### 4.1.1 画布与组卷交互

- 左侧**题型面板**：单选、多选（不定项）、判断、填空；拖拽题型到画布即生成一个「题块」。
- 画布采用**流式布局**：题块按顺序自上而下排列，可拖动调整顺序（题号自动重排）；编辑器内实时预览分页效果。
- 每个题块可设置：
  - 题干文本（支持富文本：加粗、斜体、下划线、列表、图片占位——图片资源随 AMF 打包，首版不渲染嵌入图片，仅占位符）
  - 分值、选项数量与选项文本（单选/多选）；判断题选项固定为 T/F（可改为 对/错/√/×，可配置）
  - 正确答案与记分规则（见 §4.1.2）
  - 填空题：答案类型（精确/多答案同义/正则/数值容差），作答区域高度
- **试卷模板设置**：试卷标题、副标题、考试信息栏（学科/年级/时长/注意事项）、页眉页脚、姓名栏、学号栏（答题卡模式）。
- **作答方式设置**（影响排版）：标准化答题卡作答 / 试卷直接作答。
- 保存/加载：工程以 AMF JSON 保存（含布局生成参数），可随时重新生成 PDF。

#### 4.1.2 题型与记分规则（首版）

| 题型 | 类型标识 | 作答形式 | 记分规则（可配置，括号为默认） |
| --- | --- | --- | --- |
| 单选 | `single_choice` | 涂卡 | 正确得满分，错误/未涂 0 分 |
| 多选（含不定项） | `multiple_choice` | 涂卡 | 全对得满分；漏选得部分分（默认满分×0.5）；错选/多选/未涂 0 分；也可配置为「必须全对」或「按选项计分」 |
| 判断 | `true_false` | 涂卡 | 正确得满分，错误/未涂 0 分 |
| 填空 | `fill_blank` | 手写作答 + OCR 转写 | 匹配成功得满分；支持精确/同义列表/正则/数值容差；每题可设多个空 |

#### 4.1.3 AMF JSON 导入

- 提供**正式 Schema**（见 §9.1）与校验器；导入时逐条给出可读错误（行号/字段路径/原因），不因单个错误丢弃整份文件。
- 提供示例文件（含 4 种题型）供教师/开发者参考；后续可提供「从 Word 题面转换」的辅助导入（v0.2 候选）。
- 导入成功即进入编辑器，可继续修改后重新生成 PDF。

### 4.2 两种作答模式

#### 4.2.1 模式 A：标准化答题卡作答（默认）

- 试卷只印题目，答题卡由软件根据 AMF 自动排版，包含：
  - 顶部信息区：考试标题、姓名手写栏、**学号涂卡区**（默认 6 位，每位 0–9）、页码二维码。
  - 客观题区：按题号排列的涂卡气泡（题号与试卷一致）。
  - 填空题区：带边框的作答框（固定高度，便于 OCR 裁切与行分割）。
- 答题卡版式设计参考 **Cambridge B1 Preliminary 等标化语言考试答题卡**：姓名/考号栏置顶、客观题分块编号、气泡间距规整、页脚印刷说明文字（"Use a pencil…" 之类，可配置文案）。

#### 4.2.2 模式 B：试卷直接作答

- 题目下方内嵌**作答标记块**：客观题为内联气泡行（每选项一个气泡），填空题为带边框作答框。
- 批改时按相同管线处理（标记块同样参与定位与分割），无需单独答题卡。

### 4.3 定位标记系统（软件自行设计）

> 参考 Cambridge B1 Preliminary 答题卡的「角标 + 条码区」思路，自行设计如下（详细规格见 §8.1）：

| 标记 | 数量/位置 | 作用 |
| --- | --- | --- |
| L 型角标（corner marker） | 每页四角各 1 个 | 透视校正、版面配准、页方向判定 |
| 二维码（QR） | 每页 1 个（顶部中央） | 编码考试 ID、页码、版本、校验位；用于页归属与多页合并、防错页 |
| 条码（可选） | 页脚 1 个 | 冗余定位/人工扫描枪录入，首版可选启用 |
| 学号涂卡区 | 每页头部 | 学生身份识别（多页合卷时每页重复，保证单页即可归属） |
| 作答标记块 | 每题对应 | 涂卡气泡 / 填空作答框，全部按统一网格布局 |

- 所有标记印刷为**灰度安全色**（黑色），与作答区保持最小间距，避免误识别。
- 标记尺寸与容差见 §8.1；软件对轻微污损、低对比度、±3° 内倾斜具备容错。

### 4.4 PDF 生成

| 产出 | 内容 | 说明 |
| --- | --- | --- |
| 试卷 PDF | 题目 + 作答标记（模式 B）/ 无作答区（模式 A） | 供打印分发 |
| 答题卡 PDF | 标准答题卡（仅模式 A） | 版式由 AMF 的 `answer_sheet_config` 决定 |
| 答案 PDF | 题目 + 标准答案标注 | 供教师批注/存档；答案数据同时保存在 AMF JSON 中，批改不依赖此 PDF |
| AMF JSON | 工程文件（含答案） | 批改时的权威答案来源 |

- 纸张 A4、纵向；页边距与打印安全区统一；字体嵌入（避免换机乱码）。
- 题块不可跨页截断（超长题块整体移页）；分页后回填每题的页号与块坐标到 AMF（`positions` 字段），供批改使用。

---

## 5. 功能需求详述（试卷/答题卡批改）

### 5.1 任务提交

- 教师选择考试项目 → 上传扫描 PDF（支持拖拽多文件；支持一次性提交全班多页）。
- 系统校验：页数与答题卡模板匹配性、可读性预检（是否含定位标记），给出可读警告（如「第 3 页未检测到定位标记」）。
- 批改任务后台执行，前端实时显示进度（按页推进：预处理 → 定位 → 分割 → 识别 → 判定）。

### 5.2 图像预处理管线

1. 扫描页渲染为位图（300 DPI 目标；PDF 内嵌图像直接取原图）。
2. 灰度化 → 去噪（中值/高斯）→ 自适应二值化（Otsu）。
3. **基于定位标记的倾斜校正与透视矫正**：检测四角 L 型角标 → 求单应矩阵 → 矫正到标准坐标系（消除扫描畸变）。
4. 页方向判定：根据二维码方向/角标几何关系确定页面上下（支持扫描仪自动双面产生倒置页）。

### 5.3 版面定位与题目分割

- 依据矫正后的网格与 AMF 中的 `positions`（页号 + 块矩形），为每道题提取 **ROI（Region of Interest）**：
  - 客观题：每个选项一个气泡 ROI；
  - 填空题：作答框 ROI（进一步按行投影切分为行）。
- 分割结果持久化（裁剪图缓存），供识别与人工复核回看原图。

### 5.4 客观题涂卡识别（像素分析）

- 对每个气泡 ROI 计算**填充率**（黑色像素占比）。
- 判定规则（默认值均可配置）：
  - 填充率 ≥ `fill_threshold`（默认 0.5）→ 判定为涂选；
  - 填充率落在 `(0.25, 0.5)` → **低置信**（可能是浅涂/擦除残留）→ 进入复核队列；
  - 单选组内出现多个涂选 → 低置信，进入复核。
- 输出每个选项的填充率与置信度；置信度基于「最高涂选填充率 − 次高填充率」的间隔计算。
- 该路径**不依赖 OCR**，纯像素分析，速度与准确率最高。

### 5.5 填空题 OCR 转写

- 默认**本地 OCR**（开源引擎，见 §7.2 选型）；教师可在设置中配置**外部 OCR API 或视觉 LLM API**（OpenAI 兼容格式）并指定本考试使用 API 转写。
- 转写流程：作答框裁剪 → 放大/归一化 → 行分割（投影法）→ 逐行 OCR → 后处理（拼写校正可选）→ 输出文本与置信度。
- 首版识别能力边界（须在界面明示）：**印刷体效果好；清晰手写体可用；潦草手写体准确率有限**，低置信条目一律进入人工复核，由教师对照裁剪图修正。

### 5.6 答案匹配与判分

- 匹配前归一化：Unicode NFKC、统一大小写、去首尾空白、折叠连续空白、全半角统一、按配置去除常见标点。
- 支持答案类型：精确匹配、多答案同义列表（`any_of`）、正则表达式（`regex`）、数值容差（`numeric`，如 ±0.5）。
- 按 §4.1.2 记分规则计算每题得分与总分；每题输出判定（correct / incorrect / partial / review）。

### 5.7 人工复核界面

- 列出全部 `review` 条目（默认按置信度升序）：左侧原图裁剪（放大到气泡/作答框粒度），右侧当前识别结果。
- 教师可：改判（选其他选项 / 修改转写文本 / 标记「空白」）、批量接受、跳过。
- 复核后自动重算成绩并刷新导出数据。

### 5.8 导出（统一格式）

- 单次考试导出 **一个 XLSX 工作簿**（含四张工作表，见 §9.3），同时导出批改结果 JSON（原始数据，含每题识别细节）。
- 导出内容：
  1. **学生答案数据**：学号、姓名、页码、题号、题型、学生作答（选项/文本）、标准答案、判定、置信度、得分；
  2. **学生成绩数据**：学号、姓名、每题得分、总分、满分、得分率、排名（可选）；
  3. **班级成绩报告**：人数、平均分/最高/最低/中位数/标准差、及格率、分数段分布、每题正确率、每题选项分布（客观题）；
  4. **元数据**：考试信息、模板版本、批改时间、识别引擎配置（便于审计与复现）。
- 首版保证**统一列结构与字段命名**（后续版本做存档与长期分析时直接兼容）。

### 5.9 设置（Settings）

| 设置项 | 说明 |
| --- | --- |
| OCR 引擎 | 本地（默认）/ 外部 API（自定义）；外部 API 需填 endpoint、API key、模型名，支持 OpenAI 兼容格式 |
| 视觉 LLM API | 同上，用于扫描页转写（图像 → 结构化文本），可指定仅在本地识别低置信时启用 |
| 涂卡阈值 | 填充率阈值、低置信区间 |
| 扫描偏好 | 默认 DPI、色彩模式建议 |
| 导出偏好 | 是否包含排名、是否附带原始批改 JSON |
| 隐私提示 | 配置外部 API 时显著提示「数据将发送至第三方服务」 |

---

## 6. 非功能需求

| 类别 | 要求 |
| --- | --- |
| 性能 | 单页（300 DPI）预处理+定位+涂卡识别 ≤ 5s（主流 CPU）；100 份答题卡全流程 ≤ 10 分钟；填空 OCR 按题块数量线性 |
| 准确率（验收目标） | 客观题批改准确率 ≥ 99%；定位成功率 ≥ 99.5%；印刷体填空字符准确率 ≥ 95%；清晰手写体 ≥ 80%（目标值，最终以合成样本实测为准）；需人工复核的条目 ≤ 15% 页 |
| 本地隐私 | 默认零外传；除用户显式配置 API 外无任何网络请求；本地日志不含可识别学生身份信息（姓名脱敏） |
| 可靠性 | 批改任务状态持久化（中断可续跑）；启动器崩溃可重启恢复；生成/导出失败有可读错误与日志 |
| 兼容性 | 浏览器：Chromium 内核（Chrome / Edge）；操作系统：Windows 10/11 x64（首版）；内存 ≥ 4GB（推荐 8GB） |
| 易用性 | 新用户引导（首次创建考试的示例工程）；所有关键操作可撤销/重来；中文界面 |
| 可维护性 | 模块化分层（§7.3）；统一日志；AMF schema 带版本号，向后兼容策略明确 |
| 国际化 | 首版中文界面；AMF 与导出字段名保持英文（便于工具链与后续多语言） |

---

## 7. 技术架构与实现方案

### 7.1 总体架构

```
┌───────────────────────────────────────────────────────────────┐
│ 启动器 (Launcher: Tauri/Electron 桌面壳)                        │
│  · 检查端口占用 · 拉起后端进程 · 打开默认浏览器 · 托盘图标管理     │
└──────────────┬────────────────────────────────────────────────┘
               │ 打开 http://127.0.0.1:<port>
┌──────────────▼───────────────────────────────────────────────┐
│ 前端 SPA（React + Vite，本机浏览器中运行）                      │
│  试卷编辑器（拖拽/画布/预览） · 批改任务监控 · 复核界面 · 设置    │
│  WebSocket ←→ 进度推送                                          │
└──────────────┬────────────────────────────────────────────────┘
               │ HTTP/WS (localhost)
┌──────────────▼───────────────────────────────────────────────┐
│ 主后端（Node.js / TypeScript, Fastify）                        │
│  项目管理 · AMF 校验 · 布局引擎 · PDF 生成 · 批改任务编排 ·      │
│  判定引擎 · 导出（XLSX） · 设置管理 · 任务队列/进度             │
├───────────────────────────────────────────────────────────────┤
│ 识别服务（Python, FastAPI, 本地子进程）——OCR/图像处理微服务      │
│  预处理 · 定位标记检测 · 透视矫正 · 题块分割 · 涂卡像素分析 ·     │
│  填空 OCR（PaddleOCR/Tesseract）· 外部 OCR/LLM API 代理         │
├───────────────────────────────────────────────────────────────┤
│ 本地文件系统（考试项目目录，见 §7.4）                           │
└───────────────────────────────────────────────────────────────┘
```

**架构决策**：
- 主后端与识别服务**进程分离**：识别链路（OpenCV/PaddleOCR）Python 生态最成熟，Node 负责业务编排与 Web 服务，两者通过 localhost HTTP 通信；识别服务接口抽象，可整体替换（如未来迁到 WASM/原生）。
- 若部署环境无法安装 Python，**备选方案**：识别服务下沉为 Node 侧实现（OpenCV.js / Tesseract.js / sharp），代价是识别质量与速度下降，作为 Plan B 在 PoC 阶段验证后定案（见 §12 待确认项 F-7）。

### 7.2 技术选型（决策表）

| 组件 | 推荐方案 | 备选 | 决策依据 |
| --- | --- | --- | --- |
| 前端框架 | React 18 + Vite + TypeScript | Vue 3 | 生态、拖拽库（dnd-kit）成熟 |
| UI | 自研组件 + CSS（轻量，避免重型组件库） | Ant Design | 首版界面精简，减少体积 |
| 拖拽组卷 | dnd-kit（列表重排）+ 自研画布渲染（DOM/SVG） | react-grid-layout | 流式布局需求简单，自研可控 |
| 主后端 | Node.js 20 + TypeScript + Fastify | Express | 性能与 TS 生态 |
| 识别服务 | Python 3.11 + FastAPI | — | 图像/OCR 生态 |
| 图像处理 | OpenCV (Python) | sharp (Node) | 透视矫正/轮廓检测需要 |
| 本地 OCR | **PaddleOCR（PP-OCRv4，ONNX Runtime 推理）** | Tesseract (tessdata eng+chi_sim) | 英文为主 + 需手写兜底；Paddle 对印刷体/清晰手写体质量更好；Tesseract.js 作为纯 JS 兜底 |
| 外部 OCR/LLM | OpenAI 兼容 Chat Completions / Vision API 适配层 | — | 教师自备 key；接口抽象，响应结构统一 |
| PDF 生成 | Python ReportLab | Node pdfkit | 精确定位图形/气泡/二维码坐标，ReportLab 更稳 |
| PDF→图像 | PyMuPDF (fitz) | Ghostscript | 内嵌图像直接取用，速度快 |
| 二维码 | `qrcode` (Python) + `jsQR`/OpenCV (检测) | ZXing | 生成/识别均开源 |
| 启动器 | **Tauri 2**（Rust，体积小） | Electron | 轻量目标；若团队无 Rust 经验则回退 Electron |
| 存储 | 文件系统 + JSON（无数据库） | SQLite（v0.2） | 首版数据规模小，目录即项目 |
| 导出 | Python openpyxl / Node exceljs | — | 四工作表 + 格式控制 |
| 任务队列 | 进程内队列 + 状态机（暂停/续跑） | BullMQ（无需，单机） | 单用户场景 |

### 7.3 核心模块清单（后端）

| 模块 | 职责 |
| --- | --- |
| `project-manager` | 考试项目的创建/加载/保存（目录结构见 §7.4） |
| `amf-validator` | AMF JSON schema 校验 + 可读错误（字段路径定位） |
| `layout-engine` | 题块流式排版、分页、块坐标回填（positions） |
| `pdf-generator` | 试卷/答题卡/答案 PDF（ReportLab 模板） |
| `scan-import` | 扫描 PDF 导入、页渲染、预检（标记检测、可读性） |
| `pipeline-orchestrator` | 批改任务状态机：预处理→定位→分割→识别→判定→复核→导出；进度上报（WebSocket） |
| `vision-service-client` | 调用识别服务的 HTTP 客户端（含外部 API 代理） |
| `scoring-engine` | 判分规则、归一化、答案匹配、置信度计算 |
| `review-service` | 复核队列管理、改判、重算 |
| `export-service` | XLSX（四工作表）与结果 JSON 导出 |
| `settings-service` | 设置读写（settings.json）、外部 API 配置校验 |
| `launcher-api` | 供启动器调用的本地 API（健康检查、退出） |

### 7.4 本地存储结构（考试项目目录）

```
AutoMark/
├─ launcher/                 # 启动器工程（Tauri/Electron）
├─ server/                   # Node 主后端
├─ vision/                   # Python 识别服务
├─ web/                      # 前端 SPA
├─ data/
│  └─ exams/
│     └─ <exam_id>/
│        ├─ project.amf.json      # AMF 工程（含答案与 positions）
│        ├─ generated/            # 试卷/答题卡/答案 PDF + 答案 JSON
│        ├─ scans/                # 原始扫描 PDF + 逐页 PNG 缓存
│        ├─ processed/            # 矫正图、ROI 裁剪图
│        ├─ results/              # 批改结果 JSON、复核记录
│        └─ exports/              # 导出的 XLSX
└─ settings.json              # 全局设置（OCR/API/阈值）
```

---

## 8. 核心算法与识别细节

### 8.1 定位标记规格（软件自设计）

```
A4 纵向答题卡（示意，非比例）：

┌─ L ─────────────────────── [QR 20×20mm] ────────────────── L ─┐
│ 姓名：____________   学号：[0][1][2]…[9]（每位涂一个）          │
│                                                                │
│ 1. (A) (B) (C) (D)        ← 涂卡气泡行（题号+选项气泡）        │
│ 2. (A) (B) (C) (D)                                             │
│ ...                                                            │
│ 26. ┌──────────────┐     ← 填空作答框（定高，供 OCR）          │
│     └──────────────┘                                           │
│                                                                │
├─ L ─────────────────────── [条码（可选）] ──────────────── L ─┤
└────────────────────────────────────────────────────────────────┘
```

| 元素 | 规格（默认） | 容差/备注 |
| --- | --- | --- |
| L 型角标 | 边长 12mm、线宽 2mm、距页边 10mm，四角各一 | 允许污损 ≤ 1 角（三点即可解单应）；缺失 ≥ 2 角判为不可用页 |
| 二维码 | 20×20mm，顶部居中；容错级别 M；编码 `版本1位 + 考试ID短码6位 + 页码2位 + 校验2位` | 单 QR 损坏时用角标 + 相邻页顺序兜底，进入人工指定页码 |
| 条码（可选） | Code128，页脚 | 冗余用途，默认关闭 |
| 涂卡气泡 | 圆形，直径 4mm，中心距 8mm，选项间距 8mm | 印刷误差 ±0.3mm 可接受 |
| 学号涂卡区 | 6 位 × 10 个气泡，置于页首 | 多页合卷时每页重复 |
| 填空作答框 | 高 8mm/行，宽占页宽 85% | OCR 按框裁切、按行分割 |

### 8.2 配准与透视矫正流程

1. 二值化后提取轮廓，按几何特征（直角 L 形、面积、纵横比）筛出四角角标；
2. 由角标中心点估计页方向（左上/右上/左下/右下）与粗略旋转；
3. 四点求单应矩阵，将全页映射到标准模板坐标系（模板尺寸 = 生成时记录的物理尺寸 × 目标 DPI）；
4. 用 QR 解码结果校验页归属（考试 ID + 页码），QR 失败则回退到「顺序 + 角标」推定。

### 8.3 涂卡判定算法

- 输入：气泡 ROI（标准坐标系下的圆形区域）。
- 特征：黑色像素占比 `fill`、连通域数量（橡皮擦除后的残余点会被过滤）、与邻近气泡的隔离度。
- 判定与置信度：

```
fill ≥ 0.50          → 涂选（高置信）
0.25 ≤ fill < 0.50   → 可疑（低置信 → 复核队列）
fill < 0.25          → 未涂
单选组内涂选数 > 1   → 低置信 → 复核
置信度 = 1 - exp(-k·(fill_selected - max(fill_others)))   （k 经验值）
```

- 阈值全部可在设置中调整；铅笔浅涂、橡皮残留、红笔批注等场景有专项用例（§10.4）。

### 8.4 填空 OCR 转写管线

1. 作答框 ROI 提取 → 双三次放大至字符高度 ≈ 48px → 自适应二值化；
2. 水平投影切行、垂直投影切词（英语场景按词切分）；
3. 逐行/逐词 OCR（PaddleOCR 英语模型；可叠加词典约束：试卷题干词汇表 + 常见拼写，抑制幻觉）；
4. 后处理：规范空白、过滤噪声字符（如孤立圆点）、置信度聚合；
5. 转写文本 + 置信度进入答案匹配（§8.5）；低于阈值进入复核。

### 8.5 答案匹配与归一化规则

| 步骤 | 规则 |
| --- | --- |
| Unicode | NFKC 规范化 |
| 大小写 | 统一小写 |
| 空白 | 去首尾、折叠连续空白 |
| 全半角 | 统一为半角 |
| 标点 | 按题目配置去除（默认去除句尾标点） |
| 匹配 | `exact` 精确；`any_of` 同义列表命中任一；`regex` 正则全匹配；`numeric` 数值 ± 容差 |

### 8.6 错误处理与边界情况

| 边界情况 | 处理策略 |
| --- | --- |
| 页颠倒/镜像 | 角标几何 + QR 方向判定，自动旋转 |
| 扫描缺页 | 页码序列不连续 → 警告清单，教师确认补扫或忽略 |
| 空白页 | 全局方差极低 → 标记为空白，不计入 |
| 低对比度/过暗 | 自适应阈值 + 直方图拉伸；仍失败 → 标记不可用进复核 |
| 涂卡过浅/擦除不净 | 可疑区间判定 → 复核队列（§8.3） |
| 单选多涂 | 低置信复核 |
| 填空字迹潦草 | 低置信复核；可启用外部视觉 LLM 二次转写 |
| 二维码损坏 | 角标 + 顺序兜底；人工指定页码 |
| 同一学生多页 | 按 (考试ID, 学号) 分组、按页码合并；学号缺失页进复核 |
| 红笔/批注干扰 | 仅分析标记块 ROI，且按颜色通道排除红色像素（可选开关） |
| 批改中断 | 任务状态持久化，恢复后从未完成页续跑 |

---

## 9. 数据格式定义

### 9.1 AMF（AutoMark File）JSON Schema —— 组卷工程/答案权威来源

```jsonc
{
  "version": "0.1",                    // schema 版本
  "exam": {
    "id": "exam_2026_eng_u3",          // 全局唯一（生成时自动补全）
    "title": "English Unit 3 Quiz",
    "subject": "English",              // 任意学科
    "grade": "Grade 7",
    "created_at": "2026-08-19T10:00:00+08:00"
  },
  "paper": {
    "mode": "answer_sheet",            // "answer_sheet" | "on_paper"
    "template": {
      "title": "English Unit 3 Quiz",
      "subtitle": "Time: 40 min",
      "instructions": "Read each question carefully.",
      "header": { "show_name": true, "show_student_id": true },
      "footer": "Page {page} of {pages}"
    },
    "sections": [
      {
        "id": "sec_1",
        "type": "single_choice",
        "title": "Section A: Multiple Choice",
        "instructions": "Choose ONE best answer and fill in the bubble.",
        "questions": [
          {
            "id": "q1",
            "number": 1,
            "score": 1,
            "prompt": "She ___ to school every day.",
            "options": ["go", "goes", "going", "gone"],
            "answer": { "kind": "single", "correct": 1 }
          },
          {
            "id": "q5",
            "number": 5,
            "score": 2,
            "prompt": "Which are healthy habits?",
            "options": ["Sleep early", "Skip breakfast", "Exercise", "Stay up late"],
            "answer": {
              "kind": "multiple",
              "correct": [0, 2],
              "scoring": { "full": 2, "partial": 1, "wrong": 0 }  // 漏选得 1 分，错选 0 分
            }
          }
        ]
      },
      {
        "id": "sec_2",
        "type": "true_false",
        "title": "Section B: True or False",
        "questions": [
          {
            "id": "q11",
            "number": 11,
            "score": 1,
            "prompt": "The sun rises in the west.",
            "options": ["T", "F"],
            "answer": { "kind": "tf", "correct": 1 }   // 1 = F
          }
        ]
      },
      {
        "id": "sec_3",
        "type": "fill_blank",
        "title": "Section C: Fill in the Blanks",
        "questions": [
          {
            "id": "q16",
            "number": 16,
            "score": 1,
            "prompt": "I ____ (go) to school by bus every day.",
            "answer": {
              "kind": "text",
              "match": { "type": "any_of", "values": ["go", "am going"] }
            },
            "layout": { "lines": 1 }                     // 作答框行数
          },
          {
            "id": "q17",
            "number": 17,
            "score": 1,
            "prompt": "The temperature today is ____ °C.",
            "answer": {
              "kind": "text",
              "match": { "type": "numeric", "value": 23.5, "tolerance": 0.5 }
            }
          }
        ]
      }
    ]
  },
  "answer_sheet_config": {             // 仅 mode = answer_sheet 时生效
    "page_size": "A4",
    "orientation": "portrait",
    "mark_style": "ellipse",           // 气泡样式
    "bubble_size_mm": 4.0,
    "bubble_pitch_mm": 8.0,
    "student_id": { "kind": "bubble", "digits": 6 },
    "markers": { "corners": true, "qr": true, "barcode": false }
  },
  "positions": {                       // 生成后由布局引擎回填（批改用）
    "pages": [
      {
        "page": 1,
        "markers": { "corners": [[12,12],[188,12],[12,265],[188,265]], "qr": [93,10,113,30] },
        "blocks": [
          { "qid": "q1", "kind": "bubble", "rect": [30, 60, 170, 70], "options": 4 },
          { "qid": "q16", "kind": "ocr", "rect": [30, 140, 180, 148], "lines": 1 }
        ],
        "student_id_rect": [30, 32, 90, 45]
      }
    ]
  },
  "metadata": {
    "generator": "AutoMark v0.1",
    "layout_engine": "amf-layout@0.1"
  }
}
```

> 所有坐标单位为 **mm**（物理坐标），批改时按目标 DPI 换算为像素；这样 AMF 与 DPI 解耦。

### 9.2 批改结果 JSON（每生一份，results/ 目录）

```jsonc
{
  "exam_id": "exam_2026_eng_u3",
  "student": { "id": "202601", "name": null, "pages": [1, 2] },
  "scan": { "source_file": "scans/class1.pdf", "page_indexes": [0, 1] },
  "answers": [
    {
      "qid": "q1",
      "type": "single_choice",
      "raw": { "selected": [1], "fill_ratios": [0.02, 0.93, 0.03, 0.05] },
      "confidence": 0.97,
      "verdict": "correct",
      "score": 1
    },
    {
      "qid": "q16",
      "type": "fill_blank",
      "raw": { "text": "go", "ocr_confidence": 0.88, "engine": "paddleocr" },
      "confidence": 0.88,
      "verdict": "correct",
      "score": 1
    }
  ],
  "total_score": 17.5,
  "full_score": 20,
  "review_required": false
}
```

### 9.3 导出 XLSX 工作簿（4 张工作表）

| 工作表 | 关键列 |
| --- | --- |
| Metadata | exam_id、考试标题、学科、年级、导出时间、schema 版本、识别引擎、OCR 引擎、涂卡阈值 |
| StudentAnswers | 学号、姓名、页码、题号、题型、学生作答（选项字母/文本）、标准答案、判定、置信度、得分 |
| StudentScores | 学号、姓名、每题得分（q1…qn 各一列）、总分、满分、得分率、排名（可选） |
| ClassReport | 统计指标：人数、平均/最高/最低/中位数/标准差、及格率、分数段分布；每题：正确率、选项分布 |

- 列名固定为英文（`student_id`、`question_id`…），值内文化；保证后续版本直接兼容存档。

---

## 10. 测试与验收

### 10.1 测试层次

| 层级 | 内容 |
| --- | --- |
| 单元测试 | AMF schema 校验、归一化、答案匹配、记分规则（含不定项各种组合）、置信度公式 |
| 组件测试 | 编辑器拖拽/题号重排/分值编辑、复核改判 |
| 集成测试 | 生成→PDF 结构断言（标记坐标、页数、二维码可解码）；识别服务各管线接口 |
| 端到端测试 | 组卷 → 生成 PDF → 程序化“打印/作答/扫描”仿真（渲染 + 噪声）→ 批改 → 导出 XLSX 断言 |
| 人工验收 | 真实打印 → 真实扫描 → 真实作答（铅笔/钢笔、深浅、擦除）样本集 |

### 10.2 合成扫描样本集（关键工程资产）

- 程序化生成：在生成 PDF 上叠加作答（按 AMF 随机或指定答案）、旋转 ±3°、透视畸变、噪声/污渍/低对比度/浅涂/擦除残留/红笔批注/缺角标/QR 损坏等组合。
- 样本规模建议：≥ 500 页（含 200 页手写填空，由多人书写采集）。
- 样本集随仓库版本管理，作为回归基准。

### 10.3 验收指标（KPI）

| 指标 | 目标 |
| --- | --- |
| 客观题批改准确率 | ≥ 99%（合成样本集） |
| 版面定位成功率 | ≥ 99.5%（可正确定位全部题块） |
| 印刷体填空字符准确率 | ≥ 95% |
| 清晰手写填空字符准确率 | ≥ 80%（目标值，实测后校准） |
| 需人工复核条目占比 | ≤ 15% 页 |
| 单页处理耗时 | ≤ 5s（300 DPI，主流 CPU） |
| 100 页批量全流程 | ≤ 10 分钟 |
| AMF 导入成功率 | 合法文件 100%；非法文件 100% 给出可读错误 |

### 10.4 关键用例清单（涂卡）

- 单选正确/错误/未涂/浅涂/双涂/擦除残留/铅笔 vs 钢笔/红笔覆盖；
- 多选全对/漏选/错选/不定项（部分对）各记分组合；
- 气泡边缘打印瑕疵、印刷偏移 ±0.3mm。

---

## 11. 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 手写 OCR 准确率不足 | 填空批改需大量人工复核 | 复核界面兜底；可配外部视觉 LLM 二次转写；界面明示能力边界 |
| 打印/扫描质量不稳定（教师端设备差异大） | 定位失败率上升 | 标记冗余（角标×4 + QR + 顺序兜底）、阈值可配置、预检警告 |
| 开源 OCR 对英文印刷体/手写体效果未达预期 | 验收不达标 | PoC 阶段对比 PaddleOCR/Tesseract/外部 API，提前锁定（F-7） |
| Python 运行时部署复杂（教师机） | 安装门槛高 | 启动器内嵌/自动检测 Python 环境；Plan B 为 Node 侧识别实现 |
| 浏览器兼容差异 | 编辑器/复核界面异常 | 锁定 Chromium 内核（启动器检测默认浏览器并引导） |
| 教师误操作（错传文件、错选考试） | 批改错误 | 预检 + 二次确认；批改结果可重新生成 |
| 单个大扫描件内存占用高 | 卡顿/崩溃 | 逐页流式处理、页级缓存清理 |
| 首版范围蔓延 | 延期 | 严格按 §2.2 排除项执行，新需求进入 v0.2 backlog |

---

## 12. 分阶段计划与里程碑

> 估算口径：1 名全职开发 + 0.5 名测试，约 13 周（3 个月）交付 RC。

| 阶段 | 内容 | 周期 | 里程碑/验收 |
| --- | --- | --- | --- |
| 0 | 立项文档 + 技术 PoC（定位标记检测、透视矫正、涂卡像素分析、OCR 对比） | 1 周 | M0：PoC 报告（锁定 F-7 选型） |
| 1 | 数据模型（AMF schema/校验）、布局引擎、PDF 生成、编辑器 MVP、JSON 导入 | 3 周 | M1：组卷闭环（JSON→PDF 三件套，可打印） |
| 2 | 扫描导入、预处理、定位/分割、涂卡识别、判定引擎、XLSX 导出 | 4 周 | M2：客观题批改闭环（E2E 通过） |
| 3 | 填空 OCR 转写、答案匹配（any_of/regex/数值）、人工复核界面、外部 API 配置 | 3 周 | M3：全题型批改闭环 |
| 4 | 合成样本集回归、E2E 全流程、性能调优、启动器打包、安装文档 | 2 周 | M4：RC 发布（验收 KPI 达标） |

### 待确认项（决策点）

| 编号 | 事项 | 推荐方案 | 影响 |
| --- | --- | --- | --- |
| F-1 | 学生身份识别方式 | 学号涂卡区为主（每页置顶），辅以「按扫描顺序匹配名单」兜底；姓名手写栏仅作人工参考 | 影响答题卡版式与合并逻辑 |
| F-2 | 一页扫描含多份答题卡是否切分 | 首版不支持（v0.2），预检时提示「每页应为一名学生」 | 影响扫描导入器 |
| F-3 | 涂卡填充率阈值默认值 | 0.50（可配置） | 影响识别准确率 |
| F-4 | 多页答题卡（题量超一页） | 首版支持，每页重复学号区与页码二维码 | 影响版式与合并 |
| F-5 | 最低扫描规格 | 300 DPI 推荐、200 DPI 最低，黑白/灰度 | 影响识别管线参数 |
| F-6 | 首版平台 | 仅 Windows 10/11 x64（教师机主流），macOS v0.2 | 影响启动器与打包 |
| F-7 | 本地 OCR 引擎 | PaddleOCR（推荐，PoC 验证后定案），Tesseract 备选 | 影响识别质量与部署体积 |
| F-8 | 外部 API 协议 | OpenAI 兼容 Vision 接口（endpoint/key/model 可配） | 影响设置与适配层 |

---

## 13. 附录

### 附录 A：术语表

| 术语 | 含义 |
| --- | --- |
| AMF | AutoMark File，组卷工程 JSON（含题目、答案、答题卡配置、布局坐标） |
| 题块 | 画布上一个题目及其作答区的排版单元 |
| ROI | Region of Interest，识别时从页面提取的感兴趣区域 |
| 涂卡气泡 | 客观题选项对应的圆形作答标记 |
| 定位标记 | L 型角标、二维码、条码等用于版面定位的印刷元素 |
| 填充率 | 气泡 ROI 内黑色像素占比，涂卡判定的核心特征 |
| 低置信/复核 | 识别结果不可靠，需教师人工确认的条目 |
| 模式 A / B | 标准化答题卡作答 / 试卷直接作答 |
| 启动器 | 桌面封装程序，负责拉起本地服务并打开浏览器 |

### 附录 B：参考设计 —— Cambridge B1 Preliminary 答题卡要点

- 姓名、考试中心号、考号等**信息区集中在页首**，涂卡式考号；
- 客观题**按题号分块排列**，气泡间距规整、同轴对齐；
- 页脚印刷填涂说明（如 "Use a pencil. Rub out any answer you want to change with an eraser."）；
- 版式以**机器可读**为首要目标：统一边距、无装饰、对比度高。
- 我们的设计在保留上述原则的基础上，增加了四角 L 型角标与页面二维码，以支持普通扫描仪的透视矫正与多页合卷。

### 附录 C：AMF 示例文件

仓库中随代码附带 `examples/amf_english_unit3.json`（对应 §9.1 示例），用于导入测试与文档对照。

### 附录 D：待办（文档评审后）

- [ ] 确认 §12 待确认项 F-1 ~ F-8
- [ ] PoC 报告（定位标记 + 涂卡 + OCR 对比）
- [ ] 明确 UI 线框（编辑器/批改进度/复核界面/设置）→ 设计稿
- [ ] 细化 API 接口文档（前端↔后端、后端↔识别服务）
- [ ] 确立合成样本集生成器的实现方案
